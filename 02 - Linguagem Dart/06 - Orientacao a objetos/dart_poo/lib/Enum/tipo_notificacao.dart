// oque é o enum?
// Imagina que criamos uma lista com valores padroes para os usuarios usarem
// So vamos poder usar esses valores.
// Evitando erros e padroes desconhecidos.

// Imagine que criamos uma lista com valores padrão para os usuários usarem.
//Só será possível usar esses valores, evitando erros com dados inválidos ou fora do padrão.
//O enum ajuda a manter o código mais claro, seguro e organizado.

// E como se fossem constantes...

enum TipoNotificacao{

  SMS,
  PUSH_NOTIFICACAO,
  EMAIL,
  NENHUM

  
}